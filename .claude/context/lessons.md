---
read: every session start
update: when any fix takes 2+ attempts, even if eventually solved
---

# Lessons Learned
_Claude writes here when a fix takes 2+ attempts. Prevents repeating mistakes._
_Format: `[PX.Y] [DATE] — what failed, what the fix was`_

---

## Python / Backend

<!-- [P0.2] [DATE] — example: hatchling requires [build-system] before [project] in pyproject.toml -->

---

## TypeScript / Frontend

<!-- [P0.1] [2026-04-07] — next-env.d.ts is auto-generated by `next dev/build`; never include it
     manually in tsconfig. Skeleton packages with no .ts files fail with TS18003 "No inputs found".
     Fix: add a placeholder src/types.d.ts and use "include": ["src/**/*.ts","src/**/*.tsx"] -->

<!-- [P0.3] [DATE] — example: tsconfig moduleResolution: bundler requires Next.js >=14.0.4 -->

---

## Tooling / Config

<!-- [P0.1] [DATE] — example: pnpm-workspace.yaml must be at repo root, not inside a package -->

---

## Infra / Docker

<!-- [P0.4] [DATE] — example: asyncpg needs libpq-dev in the backend Dockerfile -->
